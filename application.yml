---
# Form running gradle locally
spring:
  profiles:
    active: local
  datasource:
    url: jdbc:h2:./h2/test
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port : 8888